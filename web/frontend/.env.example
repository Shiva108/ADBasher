# API and WebSocket Configuration
# Frontend environment variables for Vite

# API Base URL - defaults to /api (uses Vite proxy in dev)
# In production, set to full backend URL like https://api.example.com
VITE_API_URL=/api

# WebSocket URL - defaults to window.location.origin
# In production, set to full WebSocket URL like https://api.example.com
VITE_SOCKET_URL=http://localhost:5000
